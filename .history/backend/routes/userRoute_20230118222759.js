const express = require('express')
const router = express.Router
const {registerUser,User}=require('')
const {protect} = require('../middleware/authMiddleware')

router.
router.

module.exports=router